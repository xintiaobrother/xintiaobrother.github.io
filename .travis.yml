
services:
  - docker
script:
  - echo docker login -u "$DOCKER_USERNAME" -password "$DOCKER_PASSWORD"
  - docker image build ./ -t xintiaobrother-blogs:1.0.0
  - docker push xintiaobrother-blogs:latest
